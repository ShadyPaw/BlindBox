# 更新日志

## 2026-09-19 · 钱包与充值 UI 原型

- 增加 `/wallet` 及桌面、手机、账户页入口，展示明确标记的示例余额和流水。
- 增加充值金额、方式、确认、处理中及成功／失败／取消交互，支持返回、关闭和重新演示。
- 隔离演示数据适配层并提供共享接口类型草案；不调用支付服务或更改真实余额。
- 增加空态、故障、重试及移动端验收用例；原站钱包的精确视觉复刻仍待参考资料对照。
- Phase 4 钱包账本、Phase 8 支付仍未开始，详见 `docs/wallet-preview.md`。

按可交付开发阶段记录。本项目仍处于开发期，下列记录不代表已上线运营或发布生产 Release。

## 2026-09-18 · Authentication + Phase 3 Catalog

本次 GitHub 更新包含上次公开前台快照之后的认证和目录后端工作。

### 新增与变更

- 邮箱密码注册、登录、退出、会话保持、受保护账户页及一次性密码重置；重置密码撤销全部旧会话。
- scrypt 密码哈希、HttpOnly Cookie、来源检查、Redis 限流及 USER／ADMIN 接口权限边界。
- 本地文件邮件和生产 SMTP 配置；邮箱尚未验证，SMTP 实际投递仍待验收。
- CatalogBox、CatalogBoxItem 及数据库迁移，导入 54 款盲盒、53 条已收集物品记录。
- `mode + slug` 联合唯一，详情 API 接受 mode，保留两种模式 Pocket Paradise 的现有链接。
- 事务化 insert-only seed：按 sourceKey 插入或跳过，重复执行不覆盖运营修改，不在启动或部署时自动运行。
- 共享 `@box/money` 工具，金额采用 BIGINT minor units，API 返回整数字符串，数量计算使用 bigint。
- `displayProbabilityPercent` 仅作参考展示，不归一化、不推算，不属于未来开奖配置。
- 公开列表、筛选、排序、分页、详情和后端相关推荐；页面运行时通过 API 读取 PostgreSQL，不回退到静态 JSON。
- CI 增加显式测试数据初始化及不可访问 API／数据库配置下的构建检查。

### 验证与修复

- 本地通过 25 项单元测试、15 项数据库／基础设施集成测试、15 项 Playwright 测试。
- 格式、lint、严格类型、Prisma 校验和离线构建通过。
- 验证数据库修改后刷新可见、同名盲盒按模式隔离、未发布商品 404、资料不完整仍可访问。
- 验证旧成功／失败请求不会覆盖最新筛选结果，服务故障显示明确错误。
- 修复认证来源判断和目录加载边界对账户页的影响，保留完整认证回归断言。

### 当前限制

- 50 款盲盒的完整物品资料仍缺失；前台尚未通过像素级 1:1 验收。
- 钱包、真实开箱、库存、支付、促销、等级、实时榜单和业务后台尚未实现。
- 注册提示已有基础文本，但醒目程度、异常文案及整体交互优化仍待专项验收。
- Docker 本机运行、生产 SMTP、生产部署和远程 CI 均不属于本轮已通过的本地验证声明。

完整阶段清单见 [项目进度与未完成事项](docs/project-status.md)。

## 2026-09-18 · 工程基础与公开前台快照

对应此前提交 `9287e93`。

- 建立四个应用、六个共享包的 pnpm TypeScript monorepo。
- 配置 Next.js、NestJS/Fastify、Prisma/PostgreSQL、Redis、BullMQ、健康检查、Compose 和 CI。
- 完成公开前台首版、消费／金币模式、列表详情、基础交互及本地素材。
- 当时通过 17 项单元测试和 8 项浏览器测试；尚无真实账户或目录业务后端。
